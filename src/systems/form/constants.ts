/** Watched by useWatch when a field has no `dependsOn`, so the hook call count
 *  stays stable without subscribing to real form state. */
export const NO_PARENT = '__form_cascade_no_parent__';

export const DEFAULT_PAGE_SIZE = 20;
export const DEFAULT_DEBOUNCE_MS = 300;
